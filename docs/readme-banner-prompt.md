# README banner generation

Generated with the built-in image generation tool using the imagegen skill.
The campaign moodboard is a style reference; `docs/logo.png` supplies the existing project mark.

## Prompt

Use case: ads-marketing.
Asset type: a finished high-resolution raster hero banner for the cursor-history GitHub README, wide landscape 3:1 composition, ideally 2400 by 800 pixels.
Primary request: create one independent, polished banner inspired by the dark README card in reference image 1. Do not reproduce the moodboard or its multiple panels.
Input images: Image 1 is the campaign moodboard, used ONLY for its dark navy, cyan and violet art direction. Image 2 is the real existing project logo: preserve its recognizable angular cube / cursor with outlined speech bubble silhouette, adapt it to cyan-violet or white on the dark background, remove its white square background, and omit its small corner sparkle.
Scene/backdrop: deep near-black navy background with extremely subtle blue-violet ambient glow. Crisp premium developer-tool editorial design, generous negative space, elegant sans-serif typography. No noisy texture.
Composition: left approximately 48 percent contains the project logo beside the wordmark, then a large two-line headline. Right approximately 48 percent contains a clean schematic showing three equal source cards in a vertical stack: Composer, Agent transcripts, Store / ACP. Each card has a tiny tasteful storage/document icon and the short format subtitle. Three cyan-violet connector lines flow from these cards to a single outlined destination card labeled cursor-history. This is a reading/discovery relationship, not a backup or migration diagram. Clear arrowheads point toward the destination only.
Text verbatim: left wordmark "cursor-history"; headline "One interface for your" on the first line and "entire Cursor history." on the second. On right source cards: "Composer" with subtitle "state.vscdb"; "Agent transcripts" with subtitle "*.jsonl"; "Store / ACP" with subtitle "store.db". Destination card "cursor-history" with small subtitle "CLI + Node.js API".
Constraints: Render all text exactly and legibly. Very large readable headline; card labels must stay readable when banner is displayed at 960 pixels wide. Keep all elements safely inside generous outer margins. Controlled cyan/violet accents, white headline, pale cool grey secondary labels. Flat front-facing composition, subtle depth on cards only.
Avoid: extra text, terminal commands, backup/migrate/restore claims or icons, performance claims, version numbers, badges, platform logos, watermark, website mockup frame, elaborate 3D, decorative circuitry, the moodboard's arrow-shaped replacement logo. Output one banner only.
